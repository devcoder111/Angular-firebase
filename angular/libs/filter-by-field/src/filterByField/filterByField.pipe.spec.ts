import { FilterByFieldPipe } from './filterByField.pipe';

describe('FilterByFieldPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByFieldPipe();
    expect(pipe).toBeTruthy();
  });
});
