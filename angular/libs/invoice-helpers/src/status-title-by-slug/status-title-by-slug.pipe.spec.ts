import { StatusTitleBySlugPipe } from './status-title-by-slug.pipe';

describe('StatusTitleBySlugPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusTitleBySlugPipe();
    expect(pipe).toBeTruthy();
  });
});
