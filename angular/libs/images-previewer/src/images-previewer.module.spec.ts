import { ImagesPreviewerModule } from './images-previewer.module';

describe('ImagesPreviewerModule', () => {
  it('should work', () => {
    expect(new ImagesPreviewerModule()).toBeDefined();
  });
});
