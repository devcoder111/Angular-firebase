import { FormatRolesPipe } from './format-roles.pipe';

describe('FormatRolesPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatRolesPipe();
    expect(pipe).toBeTruthy();
  });
});
