import { FormatRolePipe } from './format-role.pipe';

describe('FormatRolePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatRolePipe();
    expect(pipe).toBeTruthy();
  });
});
